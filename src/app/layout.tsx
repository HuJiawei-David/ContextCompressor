import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lazarus – Notebook Context Shaper",
  description:
    "Resurrect your Jupyter Notebook context. Parse .ipynb files into high-density text summaries optimized for LLM consumption.",
  keywords: ["jupyter", "notebook", "context", "llm", "gemini", "compression"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-[#0c0e14]">{children}</body>
    </html>
  );
}
